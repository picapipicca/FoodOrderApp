import React from 'react';
import classes from "./Cart.module.css";

const Cart = () => {
    return (
        <div>
            cartitems
            <div>Amount</div>
            <button>Close</button>
            <button>Order</button>
        </div>
    );
};

export default Cart;